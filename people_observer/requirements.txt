# =============================================================================
# People Observer Module - Requirements
# =============================================================================
# YOLO-based person detection with PTZ camera control.
# Tracks people across Spot's surround cameras and controls PTZ for close-ups.
#
# PLATFORM REQUIREMENTS:
#   - Python 3.9-3.10 (Spot SDK 3.7-3.10, but recommend 3.9+ for other deps)
#   - Works on Windows/macOS/Linux
#   - PyTorch auto-detects GPU: CUDA (NVIDIA), MPS (Apple Silicon), or CPU
#   - For headless servers, use opencv-python-headless instead of opencv-python
# =============================================================================

# Boston Dynamics Spot SDK
--find-links ../../spot-sdk/prebuilt
bosdyn-api==5.0.1.2
bosdyn-client==5.0.1.2
bosdyn-core==5.0.1.2

# Core CV stack
numpy==1.26.4
opencv-python==4.10.0.84
Pillow>=10.0.0
protobuf>=3.19.4,<5.0,!=4.24.0,!=5.28.0

# YOLO person detection
ultralytics>=8.0.0
torch>=2.0.0  # PyTorch backend
torchvision>=0.15.0

# TensorFlow (for Network Compute Bridge worker)
tensorflow==2.15.0
keras==2.15.0

# TensorFlow Object Detection API (for label_map_util)
# Install separately from:
# https://github.com/tensorflow/models/tree/master/research/object_detection
# Or use: pip install tf-models-official